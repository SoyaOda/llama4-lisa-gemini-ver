Llama-4-Scout-17B-16E-Instructを用いたLISAアーキテクチャのファインチューニングに関する技術的ガイド第1章 エグゼクティブサマリー1.1. プロジェクト概要本レポートは、LISA（Language-Instructed Segmentation Assistant）アーキテクチャの元々のVision-Language Model（VLM）を、より高性能なLlama-4-Scout-17B-16E-Instructに置き換えた際のファインチューニングに関する包括的なベストプラクティスを提供するものです。このアップグレードは、モデルの推論ベースセグメンテーション能力を飛躍的に向上させる機会を提供する一方で、モデルの規模と学習の安定性に関する重大な課題も提起します。1.2. コア戦略推奨される戦略は、**QLoRA（Quantized Low-Rank Adaptation）を用いたパラメータ効率的ファインチューニング（PEFT）**を中心に構築されます。この戦略は、以下の3つの主要な柱に基づいています：選択的凍結: vision_towerとmm_projectorを凍結することで、視覚コンポーネントの事前学習済み知識を保護します。ターゲットを絞った適応: Llama-4-Scout-17B-16E-Instruct言語モデルのすべての線形層にLoRAアダプタを適用し、新しいタスクへの適応能力を最大化します。積極的なメモリ最適化: 4ビット量子化、bfloat16混合精度、および勾配チェックポインティングを駆使し、17Bパラメータモデルの学習を標準的なハイエンドGPUハードウェア（例：H100/A100）で実現可能にします。1.3. 主要なパラメータ推奨値LoRA: ランク（r）を16または32から開始し、アルファ（lora_alpha）をランクの2倍に設定します。学習: AdamWオプティマイザを使用し、初期学習率を**2e-4、学習率スケジュールを線形ウォームアップ付きコサイン減衰**、有効バッチサイズを勾配蓄積によって64〜128に設定します。エポック数: 1〜3エポックで学習し、過学習を防ぐために検証損失を注意深く監視します。1.4. 期待される成果本ガイドに従うことで、実践者は安定かつ効率的なファインチューニングプロセスを実現できます。これにより、Llama-4の高度な推論能力を活用し、複雑な言語駆動型の画像セグメンテーションタスクにおいて高性能なLISAモデルを構築することが可能となります。第2章 基礎概念：新しいVLMへのLISAの適応2.1. オリジナルLISAアーキテクチャの分解LISAの核心的なイノベーションは、「推論セグメンテーション（reasoning segmentation）」を実行する能力にあります。これは、単純なオブジェクトラベルだけでなく、複雑で暗黙的、あるいは対話形式の指示に基づいて画像をセグメンテーションするタスクです 1。この機能は、「マスクとしての埋め込み（embedding-as-mask）」というパラダイムによって実現されます。具体的には、VLMの語彙に特殊な<SEG>トークンを導入し、モデルがこのトークンに対する埋め込みを出力するように学習させます。この埋め込みが後段でセグメンテーションマスクにデコードされる仕組みです 1。オリジナルの実装では、LLaVAに類似したVLMが使用された可能性が高いと考えられます。LLaVAは、ビジョンエンコーダ（CLIPなど）と言語モデル（Vicunaなど）をプロジェクション層を介して結合する構造を持っています 2。2.2. Llama-4-Scout-17B-16E-Instructへのアップグレードがもたらす影響Llama-4-Scout-17B-16E-Instructへのアップグレードは、単なるモデルの置き換え以上の意味を持ちます。それは、アーキテクチャ全体の知識基盤を再調整するプロセスです。2.2.1. 能力の向上Llama-4-Scoutは、オリジナルのLISAが基盤としていたであろうモデルよりも著しく大規模で強力なモデルです。これにより、推論、指示追従、および世界知識の能力が大幅に向上します。これは、推論セグメンテーションにおける「推論」部分にとって極めて重要です 4。2.2.2. スケールとメモリ要求の増大17Bというパラメータサイズは、完全なファインチューニングをほとんどのユーザーにとって計算コスト的に実行不可能にします。PEFTを用いたとしても、このモデルの学習には慎重なメモリ管理が不可欠です。適切な最適化なしでは、80GBのVRAMを持つ単一のH100 GPUでさえも限界に達する可能性があります 6。Llama 4 Scoutのファインチューニングには、複数のH100/H200 GPUが必要となる場合があります 4。2.2.3. 事前学習済み知識の保存このプロジェクトの成功は、重要なトレードオフにかかっています。それは、新しいLLMの強力な能力を活用しつつ、その言語的推論能力やビジョンエンコーダの基本的な視覚理解能力の「破滅的忘却」を避けることです。大規模モデルは、不注意にファインチューニングされると、事前学習で得た知識を容易に失う可能性があります。VLMにおいては、このリスクは視覚部分（見方を忘れる）と言語部分（推論の仕方を忘れる）の両方に当てはまります。近年の研究では、モデルの既存知識に根ざしていないデータでファインチューニングを行うと、幻覚（ハルシネーション）が増加する可能性も示されています 8。したがって、中心的な戦略は、力任せの完全ファインチューニングではなく、モデルのコアコンポーネントを保護しつつ、新しい強力なLLMをセグメンテーションタスクに適応させる外科的なアプローチでなければなりません。これにより、PEFTと選択的凍結戦略の重要性が、効率化のための「あれば望ましい」ものから、モデルの完全性を保つための必須要件へと格上げされます。2.3. データ品質と構造の戦略的重要性VLMのファインチューニングは、データセットの品質に極めて敏感です。高品質で、正確かつ一貫性のあるアノテーションが、成功の鍵を握ります 9。LISAの推論タスクの性質を考えると、データセットにおけるテキスト指示の品質は、セグメンテーションマスクのピクセルレベルの精度と同等か、それ以上に重要です。モデルは単にオブジェクトをセグメンテーションすることを学ぶのではなく、複雑な言語を解釈することを学びます。LISAの目標は「複雑で暗黙的なテキストクエリ」を処理することであり 1、Llama-4-Scoutはニュアンスのある命令に従うことに長けた指示チューニング済みモデルです 4。したがって、ファインチューニングプロセスは、これらの複雑な指示の理解を、<SEG>トークンを介したセグメンテーションマスクの生成にマッピングする方法をLLMに教えることに他なりません。これは、データセットの価値が、指示の豊かさと多様性、そしてそれに対応する視覚的な根拠にあることを意味します。「車をセグメンテーションせよ」のような単純な指示を持つデータセットよりも、「左に曲がろうとしているように見える車両をセグメンテーションせよ」のような指示を持つデータセットの方が価値が高いのです。この観点から、データ作成とキュレーションにおいては、単に膨大な数のサンプルを用意するのではなく、アノテーションにおける言語的な複雑さと推論ステップを優先することが推奨されます 9。その他の重要なデータ戦略は以下の通りです。少量から始め、スケールアップする: 「少量から始め、必要に応じてスケールアップする」という原則は非常に効果的です。約100サンプルの高品質な小規模データセットでも、ベースモデルに比べて大幅な性能向上が見られ、大規模な学習を実行する前に学習パイプラインを検証することができます 9。一貫したデータ構造: 視覚入力とテキスト入力の間の関連付けをより明確にするために、1つのサンプルにつき1つの画像を使用する構造が推奨されます 9。フォーマットは、アシスタントの応答における<SEG>トークンの配置を含め、LISAモデルが期待する入力形式と正確に一致している必要があります 1。第3章 パラメータ効率的ファインチューニング（PEFT）戦略：LoRAと選択的凍結3.1. 凍結戦略：視覚バックボーンとプロジェクターの保護理論的根拠: ビジョンエンコーダ（例：CLIPのvision_tower）とマルチモーダルプロジェクター（mm_projector）は、それぞれ優れた特徴抽出器およびアライナーとして、膨大なデータで広範に事前学習されています。これらのコンポーネントをファインチューニングすることは、計算コストが高いだけでなく、この重要な視覚知識の「破滅的忘却」を引き起こし、学習の不安定化を招くリスクがあります 3。主な目標は、言語モデルをセグメンテーションタスクの理解と応答に適応させることです。実装: Llama-4-Scout-17B-16E-Instructアーキテクチャ内のビジョンエンコーダとプロジェクターのモジュール名を特定します。これらは通常、vision_towerやmm_projectorといった名前が付けられています。これらのモジュール全体のパラメータに対してrequires_grad=Falseを設定し、完全に凍結する必要があります。PEFTのLoraConfigにおけるexclude_modules引数もこの目的で使用できます 10。3.2. 言語モデルのためのLoRA設定（LoraConfig）LoRAパラメータの選択は、独立した決定の集合ではなく、表現力（r）、学習強度（alpha）、および正則化（dropout、学習率）の間のバランスを取る行為です。3.2.1. LoRAランク（r）機能: rは更新行列AとBのランク（次元数）を制御し、それによって学習可能なパラメータの数を決定します。rが高いほどアダプタはより複雑なパターンを捉えることができますが、メモリ使用量と過学習のリスクが増加します 11。推奨: 17Bモデルでの複雑な推論タスクには、8のような非常に低いランクでは不十分な可能性があります。**r = 16またはr = 32**から始めるのが良いでしょう。性能が頭打ちになり、データセットが大規模で多様性に富んでいる場合は、r = 64を試すことも考えられます。128を超えるランクはほとんど必要なく、性能を損なう可能性があります 11。3.2.2. LoRAアルファ（lora_alpha）機能: lora_alphaはLoRAの更新に対するスケーリング係数です。有効なスケーリングはlora_alpha / rとなり、事前学習済みの重みに対する学習された変更の大きさを制御します 11。推奨: alphaとrの関係については議論がありますが（alpha = r、alpha = 2*r、alpha = 0.5*rなど）14、広く採用され効果的なヒューリスティックは**lora_alpha = 2 * r**と設定することです（例：r=16, lora_alpha=32）。17Bという巨大なモデルは、その重みが事前学習タスクに対して強く最適化されているため、大きな慣性を持ちます。alpha/r <= 1のような低いスケーリング係数では、モデルを効果的に操縦するには更新が微細すぎる可能性があり、より多くのエポックが必要になり、新しい知識が「洗い流される」リスクがあります。一方、alpha/r > 1（例：2）のような高いスケーリング係数は、学習された更新を増幅し、モデルがファインチューニングタスクにより注意を払うように強制します。この設定は、LoRAの更新により大きな重みを与えることで、モデルが新しいタスクをより積極的に学習するように促し、ファインチューニングにおいてしばしば望ましい結果をもたらします 11。3.2.3. ターゲットモジュール（target_modules）機能: モデルの非凍結部分（LLM）内で、どの層にLoRAアダプタを適用するかを指定します。推奨: 最高のパフォーマンスを得て、完全なファインチューニングに最も近い結果を達成するためには、Llama-4の注意機構とフィードフォワードネットワークブロック内のすべての主要な線形層をターゲットにすることが推奨されます。これには通常、["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]が含まれます 11。PEFTの初期のプラクティスでは注意層のみを対象とすることがありましたが、研究と実践が進むにつれて、フィードフォワードネットワーク層を含むより広範なパラメータにLoRAを適用することが、タスク適応の完全な複雑さを捉える上で重要であることが示されています 11。これにより、LoRAと完全ファインチューニングとの性能差が最小限に抑えられます。モジュールを削減するとわずかにメモリを節約できますが、モデルの品質を著しく低下させる可能性があるため、強く非推奨です 11。3.2.4. LoRAドロップアウト（lora_dropout）機能: 学習中にLoRAアダプタの活性化の一部をランダムにゼロに設定することで、過学習を防ぐ正則化手法です 16。推奨: その有用性については相反する証拠があります。一部の情報源は、ファインチューニングで一般的な短い学習実行にはあまり有用ではなく、0に設定するとわずかな速度最適化が可能であると示唆しています 11。しかし、他の研究では、汎化誤差を減らすための理論的利点が探求されています 17。実用的なアプローチとして、**lora_dropout = 0.05**のような小さな非ゼロ値を設定することです。これは、特にデータセットが小さいか多様性に欠ける場合に、過学習に対する低コストの保険として機能します。モデルがアンダーフィットしていると確信できる場合は、0に設定することも可能です。3.3. 最適なパフォーマンスのための高度なLoRA設定bias: このパラメータはバイアス項の学習を制御します。推奨される設定は**bias="none"**です。バイアス項を学習させると、性能向上への実用的な寄与がほとんどないにもかかわらず、学習可能なパラメータが増え、メモリ使用量が増加します 10。use_rslora: これを有効にすると、Rank-Stabilized LoRAが使用され、スケーリング係数がalpha / rからalpha / sqrt(r)に変更されます。これは特に高いランクを使用する際に学習の安定性を向上させることがあります 10。推奨: 単純化のため、最初は**use_rslora = False**で始めます。より高いランク（例：r >= 64）を試して学習の不安定性（例：損失のスパイク）が観察された場合に、use_rslora = Trueを有効にすることは有効な戦略です。表1: 推奨されるLoRA設定（LoraConfig）以下の表は、peftライブラリでLoraConfigを設定する際の推奨値をまとめたものです。パラメータ推奨値備考r32開始点として16、高度な試みとして64も検討可能。lora_alpha642 * rのヒューリスティックを維持。rに応じて調整。target_modules["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]LLMの主要な線形層をすべて対象とする。lora_dropout0.05過学習に対する低コストの正則化。bias"none"メモリ効率とパフォーマンスのためにバイアスは学習しない。task_type"CAUSAL_LM"タスクタイプを明示的に指定。use_rsloraFalser>=64や不安定性が見られる場合にTrueを試す。第4章 学習ハイパーパラメータと最適化4.1. オプティマイザの選択AdamW: Transformerモデルの学習における事実上の標準オプティマイザです。オリジナルのAdamに対する主な利点は、重み減衰（L2正則化）を勾配更新から切り離すことで、より良い汎化性能と安定した学習を実現する点です 19。paged_adamw_32bit: QLoRA（4ビット量子化）を使用する場合、オプティマイザの状態がメモリのボトルネックになることがあります。bitsandbytesライブラリのpaged_adamw_32bitオプティマイザは、ページングメモリ管理を使用してオプティマイザの状態をCPU RAMにオフロードし、メモリ不足エラーを防ぎ、より大きなモデルやバッチサイズでの学習を可能にします 4。本プロジェクトではこのオプティマイザの選択が推奨されます。4.2. 学習率とスケジューリング学習率 (learning_rate): LoRA/QLoRAのファインチューニングでは、通常、完全ファインチューニングよりも高い学習率が使用されます。複数の情報源がLlamaモデルに対して推奨する良い出発点は**2e-4**です 4。これは一般的にうまく機能する堅牢な値です。範囲としては2e-4から5e-5が考えられます 22。この比較的高めの学習率と短い学習期間の組み合わせはPEFTに特有であり、事前学習のゆっくりとした段階的な学習とは対照的に、鋭く的を絞った調整を迅速に行う戦略を反映しています。学習率スケジューラ (lr_scheduler_type): 一定の学習率は最適ではありません。学習の初期段階でモデルを不安定にしないように小さな学習率で始め、徐々に増加させた後、減衰させるべきです。ベストプラクティスは、線形ウォームアップ期間を伴う**cosine**減衰スケジュールです 23。ウォームアップ (warmup_steps or warmup_ratio): ウォームアップ段階は、完全な学習率を適用する前にモデルが新しいデータに穏やかに適応できるようにすることで、初期の発散を防ぎます。10〜100ステップのウォームアップ、または全学習ステップの**3〜10%**に相当するwarmup_ratio（0.03から0.1）が標準的なプラクティスです 4。4.3. バッチサイズと勾配蓄積モデルのメモリフットプリントによって課される小さなper_device_train_batch_sizeは、安定した学習ダイナミクスを達成するためにgradient_accumulation_stepsの使用を必須とします。これは単なる最適化ではなく、基本的な要件です。デバイスバッチサイズ (per_device_train_batch_size): これはGPUのVRAMによって制限されます。17Bモデルでは、4ビット量子化を行ってもこの値は非常に小さくなります。**1**から始め、可能であれば2や4に増やせるか試してください 4。勾配蓄積 (gradient_accumulation_steps): この技術は本プロジェクトにとって極めて重要です。重み更新を行う前に、複数の小さなフォワード/バックワードパスにわたって勾配を蓄積することで、はるかに大きなバッチサイズをシミュレートします 26。これにより、より安定した勾配が得られ、モデルの性能が向上します。有効バッチサイズ: 目標は、大きな有効バッチサイズを達成することです。有効バッチサイズ = per_device_train_batch_size * num_gpus * gradient_accumulation_stepsという式で計算されます。データセットが大規模な場合は、64、128、あるいは256の有効バッチサイズを目指してください 29。例えば、4つのGPUとデバイスバッチサイズ1を使用する場合、有効バッチサイズ64に達するにはgradient_accumulation_steps = 16が必要になります。4.4. エポック数と学習期間エポック数 (num_train_epochs): ファインチューニングにおいては、「少ない方が良い」という原則が当てはまります。モデルは既に膨大な知識を持っており、目標はそれを再学習するのではなく適応させることです。あまりにも多くのエポックで学習すると、特定のデータセットに過学習し、汎化能力が損なわれます 29。推奨: 1エポックから始めます。終了時に検証損失がまだ減少し続けている場合は、2または3エポックを試すことができます。ファインチューニングで3〜5エポック以上が必要になることは非常に稀です 22。監視: データセットの10〜20%を検証セットとして使用し、eval_lossを監視します。eval_lossの改善が止まるか、増加し始めた時点で学習を停止するのが、最適な学習期間を決定する最も信頼性の高い方法です 9。表2: 推奨される学習ハイパーパラメータ（TrainingArguments）以下の表は、Hugging FaceのTrainingArgumentsクラスを設定するためのテンプレートです。パラメータ推奨値備考output_dir"./lisa-llama4-scout-finetuned"チェックポイントと出力の保存先。per_device_train_batch_size1VRAMに応じて可能な最大値に設定。gradient_accumulation_steps16有効バッチサイズが64以上になるように調整。learning_rate2e-4LoRA/QLoRAファインチューニングの標準的な開始点。num_train_epochs21から開始し、検証損失を基に2-3まで検討。lr_scheduler_type"cosine"線形ウォームアップ付きコサイン減衰スケジュール。warmup_ratio0.03全ステップの3%をウォームアップに使用。optim"paged_adamw_32bit"QLoRA使用時のメモリ効率の良いオプティマイザ。bf16TrueH100/A100/RTX3090+/RTX4090で利用可能。logging_steps1010ステップごとにログを記録。evaluation_strategy"steps"ステップごとに評価を実施。eval_steps50評価とチェックポイント保存の頻度。save_strategy"steps"ステップごとにチェックポイントを保存。save_steps50eval_stepsと一致させることが多い。save_total_limit2ディスク容量を節約するために最新の2つのみ保存。report_to"wandb"学習の監視に強く推奨 (tensorboardも可)。第5章 メモリ管理のための必須技術これらの3つの技術（QLoRA、bfloat16、勾配チェックポインティング）は、独立した「オプション」ではなく、大規模モデルの学習を実現するための相乗効果を持つシステムです。5.1. QLoRA：4ビットNormalFloat（NF4）量子化概念: QLoRAは、4ビットなどの低精度に量子化されたモデルの上でLoRAを適用する手法です。これにより、ベースモデルをロードするために必要なメモリが劇的に削減されます 31。FP32（32ビット浮動小数点数）の17Bモデルは、重みだけで68GBのVRAMを必要としますが、QLoRA（NF4）は重みメモリを約8分の1に削減し、ベースモデルのサイズを約8.5GBにまで縮小します。これは最大のメモリ節約策です。実装: transformersライブラリのBitsAndBytesConfigを使用します。load_in_4bit=True: 4ビットでのロードを有効にします。bnb_4bit_quant_type="nf4": モデルの精度維持に優れる「NormalFloat4」量子化タイプを指定します 4。bnb_4bit_use_double_quant=True: 量子化定数に対して2回目の量子化を適用し、パラメータあたり約0.4ビットを追加で節約します。bnb_4bit_compute_dtype=torch.bfloat16: これが重要です。重みは4ビットで保存されますが、計算（行列積など）は精度の維持のために高精度（16ビットのbfloat16）で実行されます。その後、勾配は4ビットの重みに逆伝播されます 4。5.2. bfloat16による混合精度学習概念: Bfloat16（Brain Floating Point）は、32ビット浮動小数点数と同じダイナミックレンジを持ちながら精度が低い16ビット浮動小数点形式です。特にNVIDIAのAmpere（A100）やHopper（H100）シリーズなどの最新GPUにおいて、パフォーマンス、メモリ節約、学習安定性の優れたバランスを提供します 32。フォワードパスでは活性化関数（activation）が生成されますが、これらの活性化にbfloat16を使用すると、float32と比較してメモリフットプリントが半減し、これが重要な節約につながります。実装: TrainingArgumentsでbf16=Trueを設定し、モデルをロードする際にtorch_dtype=torch.bfloat16を指定します。これはQLoRAのbnb_4bit_compute_dtypeと併用されるべきです。5.3. 勾配チェックポインティング概念: 計算時間と引き換えにメモリを節約する技術です。バックワードパスでの勾配計算のためにフォワードパスですべての中間活性化を保存する代わりに、これらを破棄し、バックワードパス中に必要な場所で再計算します。これによりメモリ使用量が大幅に削減され、より大きなモデル、大きなバッチサイズ、またはより長いコンテキスト長での学習が可能になります 11。これらの節約策を講じても、長いシーケンス長や少し大きなバッチサイズでは活性化メモリがVRAMの制限を超える可能性があります。勾配チェックポインティングは、活性化メモリをクリアすることで、学習ループが（遅くはなるものの）完了することを保証する最後の手段を提供します。実装: TrainingArgumentsでgradient_checkpointing=Trueを設定します。Unslothのような一部のフレームワークは、さらなるメモリ節約を提供する最適化されたバージョン（use_gradient_checkpointing="unsloth"）を提供しています 11。第6章 LISA特有の学習設定6.1. セグメンテーションのための損失関数課題: モデルはテキスト応答とセグメンテーションマスクという2つの出力を生成します。損失関数は両方を考慮する必要があります。LISAのユニークなアーキテクチャの直接的な結果として、標準的なSFTTrainerは十分ではなく、カスタムの学習ループまたはcompute_lossメソッドのオーバーライドが必要になる可能性が高いです。推奨: 複合損失関数を実装します。画像セグメンテーションで一般的かつ効果的な組み合わせは、Dice損失とバイナリクロスエントロピー（BCE）損失の加重和です 33。BCE損失: ピクセル単位の誤差を罰し、全体的な精度に適しています。Dice損失: Dice係数（F1スコア）を直接最適化し、クラスの不均衡（例：セグメント化されたオブジェクトが背景よりはるかに小さい場合）の処理に優れ、より滑らかな損失曲線を生み出します 33。実装: 学習ループをカスタマイズして、生成されたテキストに対する標準的なクロスエントロピー損失と、<SEG>トークンの埋め込みから出力されるセグメンテーションマスクに対するこの複合Dice+BCE損失を計算する必要があります。最終的な損失は、テキスト損失とセグメンテーション損失の加重和となります。このカスタムcompute_loss関数は、テキスト部分のロジットと<SEG>トークンの埋め込みを分離し、テキストのロジットに対して標準的な言語モデリング損失を計算し、<SEG>の埋め込みをマスクにデコードし、予測マスクと正解マスクの間でDice+BCE損失を計算し、最後に2つの損失の加重和を返す必要があります。6.2. データフォーマットと<SEG>トークン重要なフォーマット: 学習データは、モデルが期待する対話形式に厳密に従う必要があります。これは通常、USER: <instruction> ASSISTANT: <textual_response> <SEG>のような形式です。オリジナルのLISAの論文でもこの構造が示されています 1。特殊トークンの扱い: <SEG>トークンは、特殊トークンとしてトークナイザの語彙に追加する必要があります。損失関数のためのラベルを作成する際、セグメンテーション損失の計算では、ユーザーのプロンプトとアシスタントのテキスト応答に対応するトークンをマスクアウト（例：-100に設定）し、セグメンテーションタスクの部分については<SEG>トークンの出力に対してのみ損失が計算されるようにする必要があります。第7章 推奨構成と実装戦略7.1. 統合：完全で実行可能な構成このセクションでは、これまでのセクションのすべての推奨事項を、単一のまとまりのある構成に統合します。これにより、実践者が構成を迅速に参照し、実装するための究極的な実用的成果物を提供します。表3: Hugging Face Trainerのための完全な推奨構成以下のPythonコードブロックは、議論されたすべてのベストプラクティスを統合した、実用的ですぐに使える実装を示しています。Pythonimport torch
from transformers import BitsAndBytesConfig, TrainingArguments
from peft import LoraConfig

# 1. 量子化設定 (Quantization Config)
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_use_double_quant=True,
    bnb_4bit_compute_dtype=torch.bfloat16
)

# 2. LoRA設定 (LoRA Config)
lora_config = LoraConfig(
    r=32,
    lora_alpha=64,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM"
)

# 3. 学習引数 (Training Arguments)
training_args = TrainingArguments(
    output_dir="./lisa-llama4-scout-finetuned",
    per_device_train_batch_size=1,
    gradient_accumulation_steps=16, # 4 GPUで有効バッチサイズ64になるよう調整
    learning_rate=2e-4,
    num_train_epochs=2,
    lr_scheduler_type="cosine",
    warmup_ratio=0.03,
    optim="paged_adamw_32bit",
    bf16=True,
    logging_steps=10,
    evaluation_strategy="steps",
    eval_steps=50,
    save_strategy="steps",
    save_steps=50,
    save_total_limit=2,
    gradient_checkpointing=True,
    report_to="wandb",
)
7.2. 段階的な実装計画環境設定: 必要なライブラリをインストールします。特にバグが知られている場合は特定のバージョンに注意します（例：transformers==4.51.0が埋め込みのバグを避けるために言及されているが、これは古い情報かもしれません）31。bitsandbytes、peft、accelerate、trlをセットアップします。データキュレーション: 約100〜200サンプルの高品質な「スモークテスト」用データセットを準備します。アノテーションが完璧で、フォーマットが正しいことを確認します 9。パイプライン検証: 小規模データセットで数百ステップの学習を実行します。ここでの目標は完璧なモデルを得ることではなく、以下の点を確認することです：QLoRAでモデルが正しくロードされるか。カスタムのデータコレータと損失関数がエラーなく動作するか。学習損失が減少するか。メモリ使用量が安定しており、制限内にあるか。スケールアップ: パイプラインが検証されたら、完全な学習データセットに切り替えます。監視と評価: Weights & Biases（wandb）やTensorBoardのようなツールを使用して、train/lossとeval/lossを綿密に追跡します。eval/lossは、学習をいつ停止するかを決定するための最も重要な指標です。チェックポイントの選択: 最良のモデルは、必ずしも最終チェックポイントではなく、eval/lossが最も低いチェックポイントです。推論と定性分析: 学習後、確保しておいたテストセットで定性的なテストを行い、複雑な推論とセグメンテーションタスクにおけるモデルの性能を「目視で」評価します。7.3. 最終的な考慮事項と今後の展望アダプタのマージ: 学習後、LoRAアダプタはmodel.merge_and_unload()を使用してベースモデルの重みにマージできます。これにより、アダプタによる推論時のレイテンシオーバーヘッドがなくなります 18。ハイパーパラメータ探索: 本ガイドは強力な出発点を提供しますが、真の最適化には、リソースが許せば系統的なハイパーパラメータ探索（例：OptunaやRay Tuneを使用）が必要になる場合があります 34。チューニングで最も影響力のあるパラメータは、通常、learning_rate、r、lora_alphaです。データ中心AI: 最終的なモデルの性能は、ハイパーパラメータの微調整よりも、データセットの改善（より良いアノテーション、より多様で複雑な指示）に対してより敏感である可能性が高いです。今後の取り組みでは、幻覚を減らすために学習データをモデルの既存知識に適応させるKnowAdaのようなデータ中心のアプローチを検討する価値があります 8。



